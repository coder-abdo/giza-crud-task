import { AddBook } from "@/components/form/addBook"

export const Add = () => {
  return (
    <main className="d-flex flex-column align-items-center">
      <h2 className="text-info fs-1 fw-bold">Add Book:</h2>
      <AddBook />
    </main>
  )
}
